/**
 * Seed — superseded by Firestore.
 * Use the Firebase Console or a dedicated seed script to populate data.
 */
function seedDatabase() {
  console.log('ℹ️  Seeding skipped — use Firebase Console or a custom seed script.');
}

module.exports = { seedDatabase };
